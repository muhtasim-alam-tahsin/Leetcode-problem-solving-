



#include <stdio.h>
#include <string.h>

int main(){
  
  int p;
  char arr1[20],arr2[20],arr3[25];
  printf("Enter first string: ");
  fgets(arr1,20,stdin);

  printf("Enter second string: ");
  fgets(arr2,20,stdin);
  // printf("Enter position to insert: ");
  // scanf("%d",&p);

  //strncpy(arr3, arr1, p); 
  //arr3[p] = '\0';
  arr1[strlen(arr1)-1] = '\0';
  strcat(arr1,arr2);
  
  // arr3[strlen(arr3)-1] = '\0';
  // strcat(arr3,arr1+p);
  printf("Merged string: %s\n",arr1);

}
